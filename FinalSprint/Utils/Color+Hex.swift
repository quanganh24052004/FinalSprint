//
//  Color+Hex.swift
//  FinalSprint
//
//  Created by EF2025 on 26/8/25.
//

import SwiftUI

extension Color {
    func toHex() -> String {
        // chuyển Color -> hex (approx); với built-in ta hardcode sẵn
        // với màu tuỳ chỉnh, có thể lưu qua UIColor.getRed(...)
        "#FF9500" // simplified; đủ dùng cho built-in
    }
}
